node_modules/
.DS_Store
tools/XHS-Downloader/
downloads/
public/media/

# Out-of-band media bundle (transferred manually, not via git)
potluck-media.zip

# Tunnel secrets (ngrok domain / token) — never commit
tools/tunnel.env
